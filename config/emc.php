<?php

declare(strict_types=1);

return [
    'saved_views' => [
        // Default number of items per page when not specified or invalid
        'default_limit' => 50,
        // Max number of items per page allowed via the API
        'limit_cap' => 100,
    ],
];
